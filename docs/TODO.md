# POPF 待办清单

状态：`2026 Reconstruction / Modern Vision Baseline Complete`

## 1. 已完成

- [x] 检查当前项目目录。
- [x] 盘点当前发现的全部文件。
- [x] 确认当前没有 `data/`、原始图片、代码、数据库或 KNIME 工作流。
- [x] 阅读 `docs/2019_paper.pdf`。
- [x] 核对论文正文、Table 1、Figure 1、Figure 2 和 Figure 3。
- [x] 记录论文明确内容、未说明内容和 4:1/80-20 划分描述的边界。
- [x] 创建项目协作规则和基础规格文档。
- [x] 完成 272 张候选图片与历史元数据的核验。
- [x] 建立 modern_v1：222 张图片、7 个实际标签、154/34/34 固定划分。
- [x] 实现直接读取 ZIP member 的 PyTorch Dataset/DataLoader。
- [x] 运行 ImageNet 预训练、冻结 backbone 的 ResNet-18 baseline。
- [x] 保存 validation checkpoint、最终模型、完整 history、test 指标、top-k 预测和 Bad Case 图。
- [x] 明确当前现代结果属于 `2026 Reconstruction`，不等同于 2019 Original。

## 2. 2019 Original 仍需补齐或核验的资料

优先级从高到低：

- [x] 当前候选历史图片包，已核验可读取 272 张图片。
- [ ] 证明当前候选图片和 XLS 是否就是论文所称 2019 MLP 最终输入。
- [ ] 2019 图片的类别/目录结构/标签表，以及 16 类完整名称和 ID。
- [ ] 约 200 张额外测试图片，或明确的测试集边界、来源和标签。
- [ ] 原始 KNIME workflow（例如 `.knwf`）或可复核的节点配置。
- [ ] KNIME 版本、Feature calculator 配置和 MLP 参数。
- [ ] 原始模型输出、评估日志、混淆矩阵或其他结果记录。
- [ ] MySQL schema、SQL dump、数据库备份或导出的表格。
- [ ] 2019 网站源代码、部署地址、接口说明或完整截图/素材。
- [ ] 图片来源和当前可用许可证明，尤其是 `paulnoll.com` 素材。
- [ ] 2019 原始数据库中的文化知识文本及其来源。

## 3. 当前关键问题

- [ ] 解释论文中“272 张 frontal images”和“数据集包含 frontal and side face types”的描述差异。
- [ ] 解释 4:1 划分与后文随机 80/20 Partitioning 的关系。
- [ ] 确认 10-fold cross-validation 的外层/内层范围。
- [ ] 确认论文所说 16 类概率与实际只训练 6 类之间的实现关系。
- [ ] 确认“至少 20 张”是每类训练样本阈值还是其他数据质量标准。
- [ ] 判断 Figure 3.2 中不可清晰读取的类别名称是否能从原始网站或项目文件恢复。

## 4. 下一步执行顺序

1. 先对 `花三块瓦脸`、`碎脸`、`象形脸` 的实际图片和标签做人工质量审计，不修改 raw。
2. 在不接触 test 的前提下，运行一个受控的 `layer4 + classification head` fine-tuning 对照实验。
3. 根据验证结果决定是否继续模型实验；不要把小 test split 的单次结果当成生产结论。
4. 单独收集和登记 2019 Original 的 KNIME、数据库、网站和额外测试集证据。
5. 在证据充分且用户明确切换阶段后，才讨论 2019 MLP 的有界重建。
6. 知识关联、用户反馈和前端实现继续保持在后续阶段。

## 5. 明确禁止事项

- [ ] 不从网上下载替代图片并当作 2019 原始数据。
- [ ] 不从常见京剧脸谱知识表补齐论文缺失的 16 类。
- [ ] 不把“约 70%”写成 2026 当前准确率。
- [ ] 不把合理猜测的 MLP 超参数写成 2019 配置。
- [ ] 不在 raw 目录中修改、裁剪、转码或覆盖文件。
- [ ] 不把现代 baseline 指代为 2019 MLP 或 KNIME 结果。
- [ ] 不使用 test split 进行调参或反复选择模型。
- [ ] 不在用户明确切换前开发前端或后端。

## 6. 2019 Original 阶段出口条件

现代视觉 baseline 不会自动补齐 2019 Original 的证据缺口。只有在原始数据边界、
关键流程资料、KNIME/MLP 配置和评估协议得到足够核验后，才可以把 2019 工作
命名为“有界复现”；否则必须继续标记为 `Unknown / To verify`。
