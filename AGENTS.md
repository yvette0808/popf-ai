# POPF 项目协作规则

本文件是 POPF（京剧脸谱数字传承）重建项目的工作约束。项目当前处于“2026 Reconstruction 现代视觉 baseline 阶段”：历史 raw 资料与现代数据集已完成核验，Dataset/DataLoader 和第一版 ResNet-18 评估已落地；后续仍必须保持 2019 Original 与 2026 Reconstruction 的证据边界。

## 项目分层

- `2019 Original`：论文或已验证的 2019 原项目资料明确记载、展示或实际提供的内容。
- `2026 Reconstruction`：本次重建新增的代码、数据处理、模型、接口、评估、知识关联、反馈和 Bad Case 分析。
- `Unknown / To verify`：资料中没有足够证据确认的内容。必须保留为未知，不得补写成 2019 的实现细节。

所有文档、代码和实验记录都必须明确使用以上标签之一。不能因为 2026 的实现方式合理，就把它写成 2019 Original。

## 不可违反的约束

1. 不虚构数据、图片、标签、文化知识、模型结果、准确率或 2019 技术细节。
2. 所有模型指标必须来自实际运行，并记录数据版本、代码版本、配置和运行时间。
3. `data/raw/` 一旦建立，原始文件只读、不可覆盖、不可重命名后丢失来源。
4. 所有清洗、转换、裁剪、灰度化、归一化和划分都必须从 raw 生成 processed。
5. 论文未说明的内容必须写成“论文未说明”或“待补证”，不能用常见做法补全。
6. 不把 2026 Reconstruction 的模型、指标或流程写成 2019 Original，不生成虚假数据，不开始前端开发。
7. 任何新增知识内容都必须有可追溯来源、审核状态和来源时间；不能凭常识代写脸谱文化知识。

## 当前已知资料

当前项目已经包含：

- `docs/2019_paper.pdf`：2019 年论文原件，是 2019 Original 的主要事实来源。
- `data/raw/`：历史原始图片、表格和其他资料，只读。
- `data/processed/`：由 raw 生成的候选数据和现代数据集。
- `src/datasets/`：直接读取 ZIP member 的 Dataset/DataLoader。
- `src/models/`、`src/training/`：2026 Reconstruction 现代视觉 baseline。
- `artifacts/`、`reports/`：实际运行的配置、权重、指标、预测和 Bad Case 产物。

2019 KNIME/MLP、网站和知识库仍未被本项目声明为已复现。详细盘点见
`docs/PROJECT_SPEC.md`。

## 证据与引用规则

在项目文档中使用以下证据标签：

- `[PAPER:P1]` 到 `[PAPER:P8]`：PDF 页码。
- `[PAPER:316]` 到 `[PAPER:322]`：论文印刷页码；仅在需要对应论文页码时使用。
- `[FIGURE]`：论文图或表中可直接观察到的内容；如果图中文字不可可靠读取，必须说明。
- `[UNKNOWN]`：当前资料无法确认。
- `[RECON-2026]`：本项目提出或实现的重建设计，不代表 2019 原项目事实。

每个结论都应尽量能回指到来源。对于从多处资料综合得到的结论，列出全部相关来源。

## 数据与实验规则

- 先登记文件路径、文件类型、大小、来源和校验和，再进入任何处理流程。
- raw 文件只做读取和校验，不在原位置修图、改名、重编码或补标签。
- processed 文件必须记录生成脚本、输入 raw 文件、处理参数和生成时间。
- 每次实验必须保存配置、数据版本、随机种子（如果使用）、训练/测试划分和评估输出。
- 2019 报告的“约 70%”只能作为历史报告值，不得当作本次复现结果。
- 本次复现得到的指标必须标记为 `2026 Reconstruction`，并与 `2019 Original` 的报告值分开。

## 当前阶段允许的工作

- 只读使用 `data/raw/`，从已核验的 processed 数据构建可复现实验。
- 运行现代视觉模型 baseline，并保存配置、权重、指标、预测和 Bad Case 证据。
- 继续完善数据质量审计、验证集实验和模型分析。
- 读取公开预训练权重，但必须记录来源、版本和校验信息。
- 维护项目文档、目录约定和待办清单。

## 当前阶段禁止的工作

- 修改、移动、重命名或覆盖 `data/raw/` 中的文件。
- 把网络搜索结果直接并入项目数据或文化知识。
- 把 2026 模型结果写成 2019 Original 结果。
- 在没有新实验配置和真实运行结果时声称模型指标。
- 训练 2019 MLP、复现 KNIME 或开发前端/后端，除非用户明确切换阶段。
- 在缺少原始证据时臆测 2019 的完整 16 类名称、数据库 schema、MLP 参数或网站后端。

## 完成阶段切换的最低条件

进入数据工程前，至少需要拿到并核验原始图片、标签/类别映射、原始测试集或其边界、原始 KNIME 工作流或足够的流程证据，以及数据库或网站资料。缺少这些资料时，只能做“有界限的 2026 Reconstruction”，不能声称复现了 2019 原实现。
